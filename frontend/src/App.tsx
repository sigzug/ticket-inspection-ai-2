import {Container} from "@chakra-ui/react";
import Nav from "./components/Nav";

export default function App() {
    return (
        <Container p={0} m={0}>
            <Nav/>
        </Container>
    );
}
